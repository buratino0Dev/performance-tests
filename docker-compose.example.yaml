version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: redis


  script:
    build:
      context: .
      dockerfile: Dockerfile.compose-example
    depends_on:
      - redis
    container_name: script
